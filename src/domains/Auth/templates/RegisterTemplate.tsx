import { IScreenProps } from '@share/utils/types/global';
import { View } from 'react-native';

interface IRegisterTemplateProps extends IScreenProps {}

export default function RegisterTemplate({ navigation }: IRegisterTemplateProps) {
  return <View></View>;
}
